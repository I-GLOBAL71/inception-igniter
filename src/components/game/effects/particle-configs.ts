// Particle configurations - now component-based
export const configs: Record<string, any> = {
  // This file is now empty as the new effects are component-based.
  // You can add new particle configurations here if needed in the future.
};